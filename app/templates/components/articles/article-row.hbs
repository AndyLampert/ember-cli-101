{{!-- this component will wrap every element. We’ll pass the necessary data to render the list of possible states and also the article --}}

<td>{{article.description}}</td>
<td>{{article.notes}}</td>
<td>{{article.createdAt}}</td>
{{!-- Ember.Select view is a helper that allows us to render a HTML select element and bind the value to a given property --}}
<td>{{view "select" content=articleStates selection=article.state}}</td>
<td>
	{{#if article.isSaving}}
		<p>Saving ...</p>
	{{else if article.isDirty}}
		<button {{action "saveArticle" article}}>Save</button>
	{{/if}}
</td>
